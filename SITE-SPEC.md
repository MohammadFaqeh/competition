# وصف تفصيلي شامل لمنصة إدارة المسابقات القرآنية — فرع الكورة

> **الغرض من هذا الملف:** وصف كامل ودقيق لما يفعله الموقع الحالي وكيف يبنى، ليُستخدم كمرجع لبناء موقع شبيه لاستخدام آخر.
> الملف مكتوب من الكود الفعلي للمشروع (تاريخ الكتابة: 2026-09-19)، لا من الذاكرة. الأجزاء الخاصة بالقرآن الكريم موسومة بـ **[خاص بالقرآن]** لتعرف ما يجب استبداله عند تغيير مجال الاستخدام، والأجزاء المشتركة موسومة بـ **[عام]** لأنها تصلح لأي نظام امتحانات/لجان.
> لا يحتوي الملف على أي مفاتيح أو كلمات مرور أو عناوين بريد حقيقية.

---

## 1. نظرة عامة

منصة ويب عربية (RTL) لإدارة **اختبارات شفهية تقيّمها لجان** من البداية للنهاية:

1. تسجيل المتسابقين (يدوياً أو باستيراد Excel) وتصنيفهم بمستويات ومراكز.
2. **سحب عشوائي عادل** لأسئلة/مواضع الاختبار لكل متسابق.
3. **تقييم إلكتروني حي** تقوم به لجنة (رئيس + عضو) وقت الاختبار: تسجيل أخطاء بأنواعها، حساب العلامة فورياً، ومقارنة رصد الرئيس برصد العضو.
4. اعتماد النتيجة النهائية، وعرضها للإدارة، وتصديرها (Excel / PDF)، وإصدار شهادات وتوصيات.
5. مراقبة حية لما يجري داخل كل لجنة، وإحصاءات، وسجل نشاط، ونسخ احتياطي.

يحتوي الموقع على **ثلاثة أنظمة امتحان مستقلة** تشترك في اللجان والواجهة:

| النظام | الغرض |
|---|---|
| **المسابقة السنوية** | مسابقة حفظ بمستويات ثابتة (9 مستويات)، علامة النجاح 75. |
| **اختبارات ديوان الحفاظ** | تصديق حفظ القرآن كاملاً عبر 4 مراحل متتالية، علامة النجاح 85، مع شهادات وتوصيات PDF. |
| **الاختبار التجريبي** | قسم تدريبي لحظي بلا حساب دخول (برمز PIN مشترك) ولا يؤثر على أي بيانات حقيقية. |

---

## 2. المعمارية والتقنيات

### 2.1 الفكرة الأساسية
- **ملفات ثابتة بالكامل بلا خطوة بناء (no build step)**: `index.html` + `app.js` + `cloud.js` + `styles.css`. تُخدَّم من أي خادم HTTP بسيط (نُشر على GitHub Pages). لا يعمل بفتح `index.html` مباشرة بسبب طلبات `fetch`.
- **لا إطار عمل (no framework)**: JavaScript صرف (vanilla)، والواجهة تُبنى بقوالب نصية (template literals) تُحقن بـ `innerHTML`، مع دالة `escapeHtml` لكل نص مُدخل.
- **`app.js`** (نحو 3200 سطر، معظمها أسطر طويلة مكثّفة) يحوي كل منطق الواجهة والحسابات. **`cloud.js`** طبقة الاتصال بـ Supabase (كائنان عامان: `window.CloudCompetition` للمسابقة السنوية و`window.DiwanCompetition` لديوان الحفاظ). **`styles.css`** نظام تصميم كامل بمتغيرات CSS.
- **الخلفية (Backend)**: Supabase (PostgreSQL + Auth + Edge Functions). كل الأمان على مستوى قاعدة البيانات (Row Level Security + دوال `SECURITY DEFINER`)، لا على منطق الواجهة.
- مكتبات خارجية مجمَّعة **محلياً** في `vendor/` (بلا CDN، ليعمل بلا اتصال): `supabase.js`، `lucide.min.js` (أيقونات)، `xlsx.full.min.js` (Excel)، `jspdf.umd.min.js` + `html2canvas.min.js` (PDF). تُحمَّل مكتبات PDF/Excel عند الحاجة فقط (`ensurePdfLibraries` / `ensureXlsx`).
- خط **Tajawal** من Google Fonts. اتجاه الصفحة RTL، اللغة العربية، مع `manifest.webmanifest` (وضع standalone، لون الثيم `#164c3b`).
- **وضع فاتح/داكن** (`setColorMode`) وشريط جانبي قابل للطي (`toggleSidebar`) ومتجاوب مع الجوال (نقطة كسر 780px).
- التصدير: Excel عبر SheetJS، وPDF عبر `html2canvas` (يرسم HTML خارج الشاشة) ثم `jsPDF`.

### 2.2 التخزين والمزامنة **[عام]**
| الوضع | أين تُحفظ البيانات |
|---|---|
| سحابي (فرع الكورة) | جدول `competition_state` (صف واحد، عمود `payload` JSONB) + جداول الجلسات. `localStorage` نسخة ثانوية فقط. |
| محلي (أي فرع آخر) | `localStorage` فقط بمفتاح `quranCompetition.localBranch.v1`. لا شيء يُرسل لأي خادم. |
| ديوان الحفاظ | جدول `diwan_state` (صف واحد JSONB) + `diwan_exam_sessions`. |
| تجريبي | `localStorage` فقط (`trialTest.v1`). |

آليات مهمة:
- **حفظ الإدارة تلقائي بعد تأخير ~450ms** من أي تعديل (`queueStateSave`)، ويرسل **الفروقات فقط** (diff) لا الحالة كاملة، ودمجاً آمناً بقفل على الخادم (`admin_save_state`) كي لا تمحو كتابةُ طرفٍ إضافةَ طرفٍ آخر.
- **استطلاع دوري (polling)**: الإدارة كل ~9 ثوانٍ، اللجنة كل ~5 ثوانٍ، مزامنة عضو اللجنة مع رئيسها كل ~1.5 ثانية، المراقبة الحية كل 4 ثوانٍ، بلاغات اللجنة كل 6 ثوانٍ. قبل تنزيل الحالة الكاملة يُستدعى `competition_state_version()` (يرجع توقيت آخر تعديل فقط) فإن لم يتغير التوقيت يتوقف الاستطلاع دون تنزيل.
- **Real-Time طبقي للجلسات**: الجلسة الجارية أو المعتمدة خلال آخر 12 ساعة (`LIVE_RECENT_WINDOW_MS`) تُجلب كل استطلاع، والأقدم تُجلب فقط عند الدخول أو التحديث اليدوي.
- **تحديث تلقائي مباشر** للإدارة قابل للتفعيل/التعطيل من الإعدادات (لهذا الجهاز فقط) لتوفير البيانات والبطارية.
- **مسودة التقييم** تُحفظ محلياً (`localStorage`) وسحابياً (`queueSessionSave`) بعد كل نقرة، وعند فتح الشاشة تُستعاد **أحدث** مسودة من المصادر الثلاثة. حماية مزدوجة (واجهة + قاعدة بيانات) تمنع مسودة متأخرة من إرجاع جلسة معتمدة إلى "قيد الاختبار".
- **خروج تلقائي بعد 30 دقيقة دون نشاط** (`IDLE_LOGOUT_MS`) لكل الحسابات السحابية.
- بيانات القرآن (نص + مواضع الأسطر + صور الصفحات) تُخزَّن بذاكرة المتصفح (`QURAN_CACHE_NAME`) وتُسخَّن مسبقاً (`prewarmQuranData`) لتعمل الشاشات بضعف الاتصال.

---

## 3. بوابة الدخول وأنماط التشغيل **[عام]**

شاشة أولى (`gatewayScreen`) بثلاث بطاقات:

1. **فرع الكورة (النظام الرسمي)** → شاشة دخول سحابي واحدة بثلاثة مسارات:
   - **دخول لجنة الاختبار**: رمز اللجنة (مثال `L01`) + PIN. يوجد حسابان مستقلان لكل لجنة (رئيس وعضو) برمزين مختلفين.
   - **دخول إدارة المسابقة**: بريد إلكتروني + كلمة مرور (Supabase Auth) — للمدير الرئيسي **ولمشرف المسابقة** معاً.
   - **دخول مسؤول فرعي**: رمز (مثال `SA-M`) + PIN.
   - كل مسار فيه **"نسيت الرمز/كلمة السر"** (انظر §11.4).
2. **استخدام محلي لفرع آخر**: شاشة إعداد أولى (اسم الدورة + اسم المشرف + رمز دخول 4 خانات فأكثر) ثم تعمل المنصة بلا إنترنت بحساب مشرف واحد.
3. **اختبار تجريبي**: يفتح القسم التجريبي مباشرة (بلا حساب، برمز PIN مشترك).

---

## 4. الأدوار والصلاحيات **[عام]**

| الدور | طريقة الدخول | ماذا يفعل |
|---|---|---|
| **المدير الرئيسي (Admin)** | بريد + كلمة مرور (Supabase Auth) | كل شيء: المتسابقون، السحب، اللجان، حسابات المسؤولين والمشرفين، الإعدادات، النسخ الاحتياطي، حذف الكل، اعتماد/رفض طلبات DR، ضبط صلاحيات كل حساب. |
| **مشرف المسابقة (Supervisor)** | بريد + كلمة مرور (يُنشأ حسابه يدوياً من لوحة Supabase Auth ثم يُربط بالتطبيق بلصق UID) | صلاحيات قريبة من المدير (حفظ الحالة، السحب، إدارة اللجان وإعدادات كل لجنة، نقل المتسابقين، المراقبة الحية). صلاحيات حساسة إضافية (تعديل نتيجة معتمدة، حذف بيانات، نقل متسابق) تُمنح بمفاتيح ON/OFF من المدير فقط. |
| **المسؤول الفرعي (Sub-admin)** | رمز + PIN (مخزّن مشفراً bcrypt) | **مقيَّد بجنس واحد (ذكور أو إناث) فقط**: يرى ويعدّل متسابقي جنسه، يجري السحب لهم، يراقب لجانهم، ينقل متسابقاً (بصلاحية `can_transfer_participant`). لا يدير اللجان ولا يحذف الجميع. مفاتيح ON/OFF من المدير: `can_edit_final`، `can_delete_data`، `can_transfer_participant` (الافتراضي كلها false). |
| **رئيس اللجنة (Chairman)** | رمز اللجنة + PIN | يبدأ اختبار متسابق، يرصد الأخطاء، **يقارن رصده برصد العضو ويعتمد النتيجة النهائية**، يغيّر الموضع أثناء الاختبار، يلغي اختباراً بدأه بالخطأ، ينهي الاختبار مبكراً، ويكتب توصية اللجنة (ديوان). |
| **عضو اللجنة (Member)** | رمز عضو مستقل + PIN | يرصد الأخطاء **بمسودة مستقلة تصل للرئيس** ولا يعتمد النتيجة. يرى تنبيه تغيير الرئيس للموضع ويعتمده. |

### 4.1 إعدادات لكل لجنة (يضبطها المدير/المشرف من قائمة اللجان)
- `active` تفعيل/تعطيل اللجنة.
- `can_edit_final`: هل تستطيع اللجنة إعادة فتح نتيجة معتمدة وتعديلها.
- `can_self_draw`: هل تستطيع اللجنة نفسها إجراء السحب — **فقط للمتسابق الذي لم تُسجَّل أجزاؤه بعد**؛ من سُجِّلت أجزاؤه يبقى سحبه للإدارة/المسؤول الفرعي.
- `show_score`: إخفاء/إظهار العلامة النهائية عن اللجنة بعد الاعتماد (الإدارة تراها دائماً).
- `show_stats_summary`: إظهار/إخفاء بطاقة إحصائية اللجنة (ممتحَنون/ناجحون/راسبون/نسبة النجاح).
- `responsible_gender`: الجنس الذي تشرف عليه اللجنة (يقيّد المتسابقين الظاهرين لها).
- `levels` / `level_names`: المستويات المسؤولة عنها. يمكن إسناد المستوى نفسه لعدة لجان عند كثرة الطلاب.
- بيانات الرئيس والعضو: الاسم، رمز الدخول، PIN (يُخزَّن مشفراً).
- **رسالة للجان (Broadcast)**: تُرسَل من الإدارة وتظهر لكل اللجان.

---

## 5. نموذج البيانات

### 5.1 حالة المسابقة (JSON واحد) **[عام]**
```
state = {
  config:       { competitionName, showFullQuranStats, showDrawsToCommittees,
                  managedCenters:[...], ... },
  participants: [ ... ],   // انظر 5.2
  draws:        [ ... ],   // انظر 5.3
  resets:       [ ... ],   // سجل إعادات
  deletions:    [ ... ]    // سجل حذف (لتفادي إعادة ظهور محذوف عند الدمج)
}
```
حالة ديوان الحفاظ بنفس الشكل مع `config.nextCertificateSeq` (عداد رقم الشهادة الداخلي، لم يعد يُطبع).

### 5.2 المتسابق (Participant) — الحقول الفعلية
`id` (نص فريد بصيغة `PREFIX-timestamp-random`)، `name`، `seat` (رقم الجلوس، فريد)، `gender` (ذكر/أنثى)، `center` (المركز)، `age`، `phone`، `branch`، `level` (عدد الأجزاء: 5/10/15/20/25/30)، `levelName` (اسم المستوى من الكتالوج)، `parts` (أرقام الأجزاء المشارك فيها)، `recitation` (الرواية)، `score` (0–100)، `gradedAt`، `scoreSource` (`electronic` | `manual`)، `assessment` (تفصيل التقييم)، `withdrawn` (منسحب)، `transferCommitteeId` (نقل يدوي لجنة بعينها)، `drRequest` (طلب تسجيل يدوي معلّق)، `manualEntryBy`.

حقول ديوان الحفاظ الإضافية: `stage` (1..4)، `usedJuz` (الأجزاء المُجازة)، `serialNumber` (الرقم التسلسلي)، `certified`، `lastGradedDrawId`.

**حالات المتسابق المشتقة** (`participantStatusOf`): `withdrawn` منسحب ← `completed` له علامة ← `drawn` له سحب بلا علامة ← `pending` بانتظار السحب. ("ممتحَن" = علامة حقيقية > 0 حتى لو انسحب لاحقاً.)

### 5.3 السحب (Draw)
`id`، `sequence` (رقم السحب المتسلسل)، `participantId`، `name`، `seat`، `center`، `age`، `level`، `eligibleParts`، `positions[]` (المواضع المسحوبة)، `createdAt`، `rerolls[]` (سجل تبديل المواضع)، `verification` (رمز تحقق).
- **رمز التحقق** = أول 12 خانة hex كبيرة من SHA-256 لـ `{id, name, معرّفات المواضع, createdAt}`.

### 5.4 التقييم (Assessment) — داخل المتسابق وداخل الجلسة
`positions[]` بعدد المواضع، كل عنصر: `{positionId, memorization, language, tajweed, hesitation, positionChange, note, completed, adopted?, changes?}`؛ إضافة إلى `actions[]` (لخاصية التراجع Undo)، `currentPosition`، `status` (`draft`/`final`)، `result`، `finalizedAt`، `committee`، `examinerDrafts: {chairman, member}`، `incomplete`، `endedEarly`، `revisions[]`، `recommendation[]` (ديوان).

### 5.5 جداول قاعدة البيانات (Supabase / PostgreSQL) **[عام]**
| الجدول | الغرض |
|---|---|
| `profiles` | ربط مستخدم Auth بدور (admin/…) + مفاتيح صلاحية (`can_edit_final`, `can_delete_data`, `can_transfer_participant`). |
| `committees` | اللجان: الاسم، المستويات، `active`، رموز ودخول الرئيس والعضو (`login_code`, `pin_hash`, `member_login_code`, `member_pin_hash`)، `chairman_name`, `member_name`، `responsible_gender`، `can_self_draw`، `show_score`، `show_stats_summary`، عدّاد المحاولات الفاشلة و`locked_until`. |
| `committee_login_sessions` | جلسات دخول اللجان (رمز مُجزّأ SHA-256، انتهاء صلاحية، `examiner_role`). |
| `sub_admins` / `sub_admin_sessions` | حسابات المسؤولين الفرعيين وجلساتهم. |
| `competition_state` | حالة المسابقة السنوية (صف واحد `id=1`، JSONB). |
| `exam_sessions` | جلسة اختبار لكل متسابق: `participant_id` (فريد)، `draw_id`، `committee_id`، `level`، `level_name`، `status` (`in_progress`/`final`)، `assessment` JSONB، `score numeric(5,2)` بين 0 و100، `started_at`، `finalized_at`. |
| `diwan_state` / `diwan_exam_sessions` | نفس الفكرة لديوان الحفاظ، لكن `draw_id` هو الفريد (متسابق واحد له عدة محاولات) وعمود `stage` (1–4) و`is_final_revision`. |
| `audit_log` | سجل النشاط (من فعل ماذا ومتى). يُحذف تلقائياً بعد 24 ساعة (`prune_old_logs`). |
| `committee_notifications` | تنبيهات نقل/تعديل متسابق للجنة. |
| `committee_issue_reports` | بلاغات اللجنة عن أخطاء بيانات المتسابق. |
| `participant_change_log` | توقيت حقيقي لتغيّر بيانات متسابق (لتنبيهات اللجنة بتوقيت صحيح). |
| `password_reset_codes` | رموز استرجاع الرمز/كلمة السر (مُجزّأة، مع عدّاد محاولات وانتهاء). |
| `backup_settings` | إعداد النسخ الاحتياطي التلقائي (تفعيل، فاصل بالدقائق ≥ 5، بريد الإشعار). |

### 5.6 دوال قاعدة البيانات (RPC) — أنماط التسمية **[عام]**
كل عملية حساسة دالة `SECURITY DEFINER` تتحقق من الهوية بنفسها. المسميات:
- `admin_*` للمدير (`admin_save_state`, `admin_create_draw`, `admin_save_committee_v3`, `admin_transfer_participant`, `admin_set_committee_final_edit/self_draw/show_score/show_stats_summary`, `admin_set_sub_admin_permissions`, `admin_delete_committee(p_purge_history)` …).
- `supervisor_*` لمشرف المسابقة (نظائر ما سبق).
- `sub_admin_*` للمسؤول الفرعي (`sub_admin_login`, `_resume`, `_logout`, `_load_state`, `_save_participants`, `_create_draw`, `_list_sessions`, `_list_live_sessions`, `_transfer_participant`, `_assign_participant_to_committee`, `_list_issue_reports`, `_resolve_issue_report`, `_delete_participant_session`).
- `committee_*` للجان (`committee_login`, `_resume`, `_logout`, `_load_state`, `_list_sessions`, `_list_live_sessions`, `_get_session`, `_claim_student`, `_save_session`, `_replace_position`, `_cancel_session`, `_create_draw`, `_report_issue`, `_list_notifications`, `_lookup_change_times`, `_used_position_ids`, `_used_positions_detailed`).
- `diwan_admin_*` و`diwan_committee_*` لديوان الحفاظ (نفس الفكرة: `diwan_admin_save_state`, `diwan_admin_create_draw`, `diwan_admin_transfer_participant`, `diwan_admin_delete_participant_draw`, `diwan_committee_claim_student`, `_save_session`, `_replace_position`, `_cancel_session` …).
- دوال مساعدة: `current_user_role()`, `committee_from_token()`, `committee_role_from_token()`, `competition_state_version()`, `diwan_state_version()`, `login_lockout_threshold()/duration()`, `record_transfer_notifications()`.

**ملفات SQL** (نحو 45 ملفاً في `supabase/`) تُنفَّذ **بترتيب محدد** لأن كل ملف يعيد تعريف دوال سابقة؛ أهم قاعدة تعلمناها: **عند إعادة تعريف دالة يجب نسخ كل حقولها من آخر نسخة فعلية لا من نسخة وسيطة**.
Edge Functions (في `supabase/functions/`): `auto-backup` (نسخة JSON دورية بالبريد عبر Cron) و`password-reset` (تحديث PIN/كلمة السر بمفتاح service_role بعد التحقق من الرمز).

---

## 6. المسابقة السنوية

### 6.1 المستويات **[خاص بالقرآن — يُستبدل بمستويات مجالك]**
تسعة مستويات ثابتة (`LEVEL_CATALOG`):

| المعرّف | الاسم | الأجزاء |
|---|---|---|
| L1 | حفظ القرآن كاملاً بغير رواية حفص | 30 |
| L2 | حفظ القرآن كاملاً برواية حفص | 30 |
| L3 | حفظ 25 جزءاً | 25 |
| L4 | حفظ 20 جزءاً | 20 |
| L5 | حفظ 15 جزءاً | 15 |
| L6A / L6B | حفظ 10 أجزاء (أقل من 20 سنة / أكبر) | 10 |
| L7A / L7B | حفظ 5 أجزاء (أقل من 15 سنة / أكبر) | 5 |

- عدد الأسئلة (المواضع) الافتراضي لكل مستوى: `{3:3, 5:3, 7:4, 10:5, 15:8, 20:10, 25:13, 30:15}`، ويمكن تعديله لكن **لا يتجاوز 15 ولا عدد الأجزاء المختارة**.
- علامة النجاح **75** (`PASS_SCORE`).
- يُطابَق اسم المستوى القادم من Excel مع الكتالوج بتطبيع المسافات والهمزات (`matchLevelCatalog`، `normalizeHeader`).

### 6.2 إدارة المتسابقين (شاشة "المتسابقون") **[عام]**
- **إضافة/تعديل** متسابق بنموذج؛ **رقم الجلوس** فريد (يُرفض التكرار)، ويُقترح تلقائياً التالي.
- **استيراد Excel/CSV متعدد الشيتات**: يتجاهل شيت اسمه "تعليمات"؛ يتعرف على الأعمدة بأسماء عربية بديلة متعددة (الاسم/اسم المتسابق/الطالب…، الجنس، المركز، رقم الجلوس/المتسابق، العمر، المستوى/الفئة، الأجزاء…) بمطابقة مطبَّعة. **المركز يُقرأ حصراً من عمود "المركز"** وليس من اسم الشيت. صف بلا مركز يُستورد ويُعلَّم بوضوح. يُستنتج المستوى من عمود المستوى أو من عمود معلَّم بعلامة. خيار لشمل/استبعاد مستوى القرآن الكامل والروايات الأخرى. تقرير مفصل بعد الاستيراد (أُضيف/حُدّث/مرفوض بسبب رقم جلوس مكرر…). قالب جاهز `participants-template.xlsx/csv`.
- **قائمة المراكز المُدارة** (الإعدادات): تُضاف مسبقاً قبل أي متسابق؛ المركز الافتراضي "مجتمع محلي".
- **الفلاتر** (تتذكر اختيارها بين الجلسات): الحالة، الجنس، المركز، المستوى، اللجنة، بحث بالاسم/الرقم/المركز؛ مع عدّاد "س من ص متسابق" وتقسيم صفحات.
- **الانسحاب**: تبديل حالة "منسحب" لمتسابق (مع بقاء علامته إن وُجدت حقيقية).
- **نقل متسابق إلى لجنة أخرى** (`transferCommitteeId`): يختفي من لجنته الأصلية ويظهر عند اللجنة الجديدة، ويُتجاوز فحص تطابق المستوى عند الحجز. تُسجَّل تنبيهات نقل ثابتة على الخادم للجان المعنية.
- **حذف** متسابق أو "حذف الجميع" (بمعرّفات حذف تمنع الرجوع عند الدمج).
- **تحديد لجنة كل متسابق** (`resolveParticipantCommittee`): اللجنة الحالية = اللجنة المنقول إليها، وإلا أول لجنة فعالة تطابق مستوى/اسم مستوى المتسابق وجنسه (لجنة `responsible_gender`). من امتحنه فعلاً يبقى منسوباً للجنة التي امتحنته.

### 6.3 توليد بنك المواضع **[خاص بالقرآن — الفكرة قابلة للتعميم كـ"بنك أسئلة"]**
- المصدر: نص القرآن العثماني (Quran Foundation Content API v4) بترقيم مصحف المدينة: 6236 آية، 114 سورة، 604 صفحة، 30 جزءاً. يُتحقق منه عند كل تشغيل (`validateQuranData`: عدد الآيات/السور، عدم تكرار المفاتيح، سلامة أرقام الجزء/الصفحة، وجود النص).
- **الموضع** = مقطع متتابع من آيات كاملة يشغل **8 أسطر من المصحف بالضبط** (يُسمح بـ9 فقط إن لم تنتج نهاية تعطي 8)، يبدأ وينتهي عند حدود آيات صحيحة. المواضع **متتابعة غير متداخلة** داخل كل جزء (`buildCandidates`). آيات قصار سور الجزء 30 (السور القصيرة) تُنهى عند نهاية آية السورة. آية تتجاوز 9 أسطر وحدها تُتخطى.
- كل موضع يحمل: `id`، `juz`، السورة والآية بداية ونهاية، الصفحة بداية ونهاية، `lineCount`، `lineSegments` (مقاطع الأسطر لكل صفحة لعرضها كصورة).
- لعرض الموضع أثناء الاختبار: **604 صورة صفحة مصحف** في `assets/quran-pages/` تُقتطع مقاطعها بحسب `lineSegments`.

### 6.4 السحب العادل **[عام في فكرته]**
- **مولّد عشوائي آمن**: `crypto.getRandomValues` مع rejection sampling (بلا انحياز modulo) وخلط Fisher–Yates (`secureShuffle`)، **لا `Math.random`**.
- الخوارزمية: يتحقق أن الأجزاء المسجَّلة للمتسابق تساوي مستواه؛ يختار `عدد الأسئلة` جزءاً **مختلفاً** عشوائياً من أجزائه (من الأجزاء التي لها مواضع)، ثم **موضعاً عشوائياً واحداً من كل جزء**، ويرتّبها بحسب رقم الجزء.
- **رمز تحقق** لكل سحب (انظر 5.3)، وشاشة "كشف السحب" بعدّ تنازلي 3 ثوانٍ، وورقة مواضع قابلة للطباعة/الحفظ PDF.
- **سحب للجميع (Bulk)**: ينفّذ السحب لكل من أجزاؤه مسجَّلة ويتخطى الباقي بتقرير واضح.
- على السحابة السحب **عملية ذرّية بالخادم** (RPC واحدة `*_create_draw`) فلا يمكن لطرفين سحب المتسابق نفسه. **من يملك صلاحية السحب**: المدير، المشرف، المسؤول الفرعي (لجنسه)، واللجنة بصلاحية `can_self_draw` بالشرط المذكور.
- **تبديل موضع أثناء الاختبار** (رئيس اللجنة فقط): يُخصم **10 علامات** ويُختار موضع عشوائي **مختلف من الجزء نفسه** (لا الموضع الحالي ولا مواضع أخرى من السحب نفسه)، **بحد أقصى مرتين لكل متسابق** عبر كل مواضعه. يجري ذلك على الخادم (`*_replace_position`) ويُسجَّل في `draw.rerolls` وسجل النشاط.
- ملاحظة اتساق: نص شاشة السحب يقول "لا تتكرر مواضع الدورة"، لكن الكود الحالي يختار عشوائياً بانتظام من مواضع الجزء **دون منع التكرار بين متسابقين مختلفين** (دوال جلب "المواضع المستخدمة" موجودة في `cloud.js` لكن غير مستهلكة حالياً في `app.js`). إن أردت منع التكرار في مشروعك الجديد فاجعله متطلباً صريحاً.

### 6.5 الاختبار الإلكتروني (شاشة اللجنة) **[عام في بنيته]**
**تدفق اللجنة:**
1. تفتح قائمة "متسابقو لجنتي" (بحث + فلتر حالة: لم يتم السحب / جاهز / قيد الاختبار / مكتمل + فلتر مركز)، وبطاقة إحصاء اللجنة، وتنبيهات اللجنة (إضافة/إزالة/تحديث بيانات متسابق مع توقيت حقيقي).
2. **"بدء الاختبار"**: تظهر شاشة **تأكيد بيانات الطالب** (الاسم/المركز/المستوى/الأجزاء) للتحقق من مطابقته للحاضر. إن وُجد خطأ: **"إبلاغ عن خطأ"** يرسل بلاغاً فورياً للإدارة/المشرف/المسؤول الفرعي المختص ولا يبدأ الاختبار.
3. **الحجز الذري** (`*_claim_student`): متسابق واحد لدى لجنة واحدة فقط؛ لجنة لا تبدأ متسابقاً جديداً وعندها متسابق "قيد الاختبار"؛ لا تمتحن لجنة مستوىً غير مخصَّص لها (إلا بنقل صريح).
4. **شاشة الاختبار** (ملء الشاشة): رأس فيه اسم المتسابق والمستوى ورقم السحب، **صور صفحات المصحف** للموضع الحالي (مع إمكان التحرك بين الصفحات)، شريط تنقل بين المواضع (نشط/مُنهى)، ولكل موضع أزرار **+ / −** لكل نوع خطأ، خانة **ملاحظة**، زر **"إنهاء هذا الموضع"**، زر **تراجع (Undo)** (بسجل `actions`)، ملخص حي للعلامة والخصم، **مؤقت مساعد 20 ثانية** اختياري بجرس (لا يُحفظ ولا يؤثر على العلامة)، تحذير عند بلوغ سقف التجويد.
5. **قواعد الخصم:**

| نوع الخطأ | الخصم |
|---|---|
| خطأ الحفظ | 2 |
| خطأ اللغة | 2 |
| خطأ التجويد | 1 (سقف **10** أخطاء تجويد مجمَّعة لكل ممتحِن، وبعدها يُسجَّل الباقي ضمن "التردد") |
| التردد | 0.2 لكل مرة |
| تغيير الموضع | 10 |

العلامة = `max(0, 100 − مجموع الخصومات)` مقرّبة لخانتين، والنجاح `العلامة ≥ حد النجاح`. تظهر **تحذير فشل مبكر** حين تصير العلامة أدنى من حد النجاح، ويظهر للرئيس زر **"إنهاء الاختبار"**.
6. **رئيس وعضو:** لكل منهما مسودة رصد مستقلة (`examinerDrafts.chairman/member`). العضو يزامن كل ~1.5 ثانية: يرى إن أنهى الرئيس الاختبار، ويُنبَّه إن غيّر الرئيس موضعاً ("يمكنك اعتماد التغيير")، وزر "تغيير الموضع" عنده **مقفل** حتى يغيّر الرئيس ذلك الموضع (فيعتمد العضو التغيير).
7. **المراجعة والاعتماد (الرئيس فقط):** جدول **مواضع الاختلاف فقط** بين رصده ورصد العضو، وفيه يُدخل الرئيس **عدد الأخطاء المعتمد** (لا الخصم) لكل نوع مختلَف عليه، والنظام يحسب الخصم ويحدّث العلامة والنتيجة فورياً. ثم "اعتماد النتيجة" يكتب الجلسة `status='final'` بالعلامة، ويُحفظ اسم اللجنة والرئيس والعضو.
8. **إنهاء مبكر** ("غير مكتمل"): تسجَّل النتيجة بحالة `incomplete` بدل علامة رقمية (تبقى قيمة رقمية داخلياً بسبب قيد قاعدة البيانات، والعرض فقط يقول "غير مكتمل").
9. **إلغاء اختبار** بدأته اللجنة بالخطأ (الرئيس فقط، وقبل الاعتماد) يعيد المتسابق إلى "جاهز للاختبار" دون المساس بالسحب.
10. **إعادة فتح نتيجة معتمدة** وتعديلها متاح فقط بصلاحية `can_edit_final`، وتُسجَّل في `revisions`.
11. الجلسة تسجّل مدة الاختبار (من البدء إلى الاعتماد) لعرضها في شاشة "مدة الاختبار".

### 6.6 التسجيل اليدوي الطارئ (DR) **[عام]**
مسار منفصل تماماً لاستخدامه عند تعطّل النظام أثناء الاختبار: يُطلب تسجيل علامة يدوية لمتسابق بسبب موضّح (`drRequest`)، ويعتمدها أو يرفضها (أو يعتمد الكل دفعة واحدة) **المدير أو المشرف أو المسؤول الفرعي الذي عنده صلاحية `can_edit_final`** (`canHandleDrRequests`)، فتُسجَّل العلامة بـ `scoreSource='manual'` مع اسم من طلبها. يمكن إلغاء التسجيل اليدوي فيعود المتسابق لـ"بانتظار العلامة".

### 6.7 اللوحات والتقارير **[عام]**
- **نظرة عامة (Dashboard)**: مؤشرات (إجمالي المتسابقين، ممتحَنون، منسحبون، ناجحون، راسبون) مفصّلة ذكور/إناث، حلقة نسبة النجاح (الكل/ذكور/إناث)، توزيع حسب المستوى، **نطاق عرض**: تراكمي أو **يوم محدد** (أرقام الامتحان تُفلتر بـ `gradedAt`؛ الإجمالي والانسحاب يبقيان تراكميين)، وبلاغات اللجان.
- **سجل السحوبات**: كل النتائج المثبتة وإعادات السحب بالتاريخ والوقت، بحث، تصدير Excel، حذف سحب أو الكل.
- **مدة الاختبار**: مدة كل متسابق داخل لجنته (فلتر لجنة، تصدير Excel).
- **المراقبة الحية** (المدير/المشرف/المسؤول الفرعي): قائمة اللجان؛ باختيار لجنة يُرى من يُختبر الآن وأي موضع وصل وأخطاء كل موضع لحظة بلحظة **دون أي إشعار لهم**. تحديث كل 4 ثوانٍ.
- **الإحصاءات**: توزيع استخدام المواضع على أجزاء القرآن، **فحص العشوائية** (محاكاة 100,000 سحب وحساب أقصى انحراف عن التوزيع المتساوي)، أداء كل لجنة (توزيع حسب اللجان مع فلتر المركز)، و**مقارنة علامات اللجان** (علامة الرئيس/العضو/المعتمدة، تنزيل Excel، حذف سجلات المعروض) للإدارة والمشرف فقط.
- **الإعدادات**: اسم المسابقة، إظهار مستوى القرآن الكامل بالإحصاءات، **إظهار/إخفاء مواضع السحب عن اللجان** مؤقتاً دون حذفها، تحديث تلقائي، المراكز المُدارة، إدارة اللجان، حسابات المسؤولين الفرعيين، حسابات المشرفين، طلبات DR، سجل النشاط، **نسخة احتياطية JSON** (تنزيل/استعادة)، قالب Excel، **بدء دورة جديدة**، إعدادات النسخ الاحتياطي التلقائي.

### 6.8 التصدير **[عام]**
- **Excel**: النتائج والترتيب (شيت لكل مستوى، أو شيت واحد للكل)، مع نافذة **فلترة قبل التنزيل** (مستويات، جنس، مراكز)؛ أعمدة: المسابقة، المشارك، الجنس، رقم المتسابق، الهاتف، الفرع، المركز، الأجزاء، المستوى، الرواية، علامات التصفية، النتيجة (ناجح/راسب/غير مكتمل)، الحالة…
- **PDF**: ورقة نتيجة المتسابق، **حفظ السحب للطلاب** (دفعة)، **بطاقات "حفظ للجمعية"** (نموذج الجمعية بشبكة الأجزاء المظلَّلة وتفقيط الأرقام بالعربية)، حسب فلاتر.

---

## 7. اختبارات ديوان الحفاظ (نظام مراحل) **[خاص بهذا المشروع، مع فكرة مراحل قابلة للتعميم]**

**الفكرة:** كل متسابق "حافظ كامل" بالتعريف والاختبار غرضه التصديق. يمرّ بـ **4 مراحل متتالية**: الاختبار الأول (10 أجزاء) → الثاني (10 أجزاء جديدة) → الثالث (الـ10 المتبقية) → **النهائي** (القرآن كاملاً).

| المرحلة | ما يُسحب |
|---|---|
| 1، 2، 3 | يختار المسؤول **10 أجزاء بالضبط** (الأجزاء المُجازة سابقاً معطّلة ومظلَّلة، وتُقفل تلقائياً على المتبقية في المرحلة 3)، ويُسحب **موضع واحد عشوائي من كل جزء** = 10 مواضع. |
| النهائي (4) | القرآن كاملاً تلقائياً: **18 موضعاً** (6 من كل ثلث: 1–10، 11–20، 21–30). |

- **علامة النجاح 85** (`DIWAN_PASS_SCORE`)؛ الرسوب يُبقي المتسابق بمرحلته ويسمح **بإعادة المحاولة بلا حد أقصى**. النجاح يرحّل الأجزاء إلى `usedJuz` ويرقّي المرحلة تلقائياً؛ النجاح بالنهائي يجعله `certified`.
- **كل محاولة = سحب + جلسة مستقلة**، وتاريخ المحاولات محفوظ ولا يُمحى (نافذة "سجل المحاولات" لكل متسابق مع زر شهادة للناجحة وتوصية للراسبة).
- اللجان مشتركة تماماً مع المسابقة السنوية (نفس الحسابات) بتبويب مستقل في شاشة اللجنة، وتحديث يدوي فقط.
- **الرقم التسلسلي** (`serialNumber`): يُدخله المسؤول يدوياً (لا توليد)، **إجباري وفريد**، ويُشترط وجوده قبل السحب وقبل أن تحجز اللجنة جلسة جديدة. يُطبع بخانة "الرقم التسلسلي" في الشهادة والتوصية. (لا يوجد "رقم شهادة" مطبوع.)
- **شاشة المتسابقين = أربعة صناديق** (الاختبار الأول، الثاني، الثالث، النهائي/القرآن كاملاً) بدل الجدول: كل صندوق يعرض عدد متسابقيه وحالاتهم (بانتظار الأجزاء / بانتظار اللجنة / راسب / حافظ معتمد) وبطاقة لكل متسابق بإجراءاته. فلاتر: الحالة، الجنس، المركز، اللجنة + بحث (لا فلتر مرحلة). استيراد/تصدير Excel (بعمود الرقم التسلسلي)، تصدير PDF مجمَّع.
- **الانتقال بين المراحل تلقائي** عند دمج نتائج اللجان (نجاح ← المرحلة التالية، رسوب ← نفس المرحلة مع إعادة الاختبار)، وللإدارة **نقل يدوي لأي مرحلة** (`stageEnteredAt`: يبدأ المتسابق المرحلة من جديد بلا سحب حالي وتبقى محاولاته بالسجل ولا تُعاد نتائج قديمة).
- **منتقي الأجزاء**: الأجزاء التي سبق اختبار المتسابق بها تُعلَّم بـ«سبق» فقط ويمكن اختيارها مجدداً (لإعادة اختبار أو اختبار جديد)؛ لا منع.
- نقل متسابق بين اللجان (زر مستقل عن نقل المرحلة).
- **الشهادات والتوصيات (PDF بصفحة A4 كاملة)**: تُبنى على **قوالب رسمية مصوَّرة** (`assets/diwan-cert-10juz.jpg`، `diwan-cert-full.jpg`، `diwan-rec-stage.jpg`، `diwan-rec-full.jpg` بدقة 1241×1754) تُستخدم **كخلفية كما هي**، وتُوضع فوقها حقول نصية بإحداثيات القالب بنسب مئوية (`DIWAN_DOC_GEOMETRY`):
  - جدول المعلومات: اسم الطالبة، الرقم (رقم الجلوس)، المركز، المستوى، تاريخ الاختبار.
  - **شبكة 30 خلية** للأجزاء تُرسم كاملة فوق القالب: **المظلَّل فقط الأجزاء التي شاركت بها المحاولة فعلاً** (من `draw.eligibleParts`)، والباقي بلا تظليل (النهائي: كل الثلاثين).
  - العلامة من 100 (أو "غير مكتمل")، والنتيجة (ناجح/غير ناجح مطبوعة في القالب نفسه).
  - في الشهادة: الاسم والمركز داخل جملة الشهادة، والتاريخ (يوم/شهر/سنة) عند سطر "في دير أبي سعيد بتاريخ"، والرقم التسلسلي أسفل الصفحة.
  - الأسماء الطويلة يصغر خطها تلقائياً بحسب عرض الخانة.
  - **الشهادة** لا تُصدر إلا لنتيجة معتمدة `≥ 85` وغير "غير مكتمل"؛ **التوصية** لا تُصدر لنتيجة ناجحة (تُرفض في الحالتين على مستوى الدالة أيضاً).
  - قالب الشهادة: "عشرة أجزاء" للمراحل 1–3، و"القرآن كاملاً" للنهائي. قالب التوصية بعنوان فرعي يتبع المرحلة (الأول/الثاني/الثالث/القرآن كاملاً).
  - **التوصية**: يكتبها **رئيس اللجنة** قبل اعتماد نتيجة غير ناجحة (أو عند الإنهاء المبكر) على شكل **نقاط مرقّمة** (حتى 5 نقاط، كل نقطة سطر واحد بحد 70 حرفاً ليناسب أسطر القالب المنقّطة)، وتُحفظ داخل تقييم الجلسة (`assessment.recommendation`) فترتبط بالمتسابق والمحاولة والعلامة. بعد الاعتماد يظهر لرئيس اللجنة زر "تنزيل التوصية PDF".
  - القوالب الحالية بصيغة المؤنث ("الطالبة"، "ونوصيها") لأن الديوان للطالبات حالياً؛ للذكور يلزم قالب مذكّر مستقل.
  - ثلاثة أسطر أسفل صندوق التوصية ("الأجزاء التي تحتاج إلى مراجعة"، "موعد إعادة الاختبار المقترح"، "ملاحظات") تُترك فارغة للكتابة اليدوية.

---

## 8. الاختبار التجريبي **[عام]**
- قسم تدريبي مستقل، **بلا حساب**، يُفتح من البوابة برمز PIN مشترك (يُعيَّن ويُغيَّر من داخله).
- بيانات لحظية في `localStorage` فقط، غير مرتبطة بأي دورة.
- يُضاف متسابق، ويختار المستخدم **أي عدد من الأجزاء وأي عدد من المواضع داخل كل جزء**، ثم يُجرى تقييم إلكتروني كامل (نفس شاشة الاختبار) ويُعتمد.
- **تُحذف كل تفاصيل السحب والتقييم فور الاعتماد**، ويبقى الاسم والمستوى والعلامة فقط. من يُضاف يظهر ضمن "لجنة اختبار مؤقتة".

---

## 9. سجل النشاط والإشعارات **[عام]**
- `audit_log`: كل عملية مهمة (دخول ناجح/فاشل/مقفل، سحب، اعتماد، تبديل موضع، نقل، حذف، تعديل نتيجة، إلغاء، طلبات DR…) بمنفّذها وتفاصيلها. **يُحتفظ بآخر 24 ساعة فقط** (تنظيف تلقائي).
- تنبيهات اللجنة: إضافة/إزالة متسابق من لجنتها، تحديث بياناته، نقله — مخزّنة على الخادم (`committee_notifications`) فتصل حتى لو فُتح الحساب من جهاز آخر، مع بقاء نسخة محلية "مقروء/غير مقروء".
- بلاغات اللجنة عن بيانات خاطئة تظهر بلوحة الإدارة مع زر "تم الحل".

---

## 10. النسخ الاحتياطي **[عام]**
- **يدوي**: تنزيل نسخة JSON كاملة واستعادتها (الوضع المحلي خصوصاً).
- **تلقائي دوري** (الإدارة الرئيسية فقط): تفعيل/تعطيل وفاصل بالدقائق (≥ 5)، تنفّذه Edge Function بجدولة Cron من خوادم Supabase (تعمل حتى مع إغلاق المتصفح) وترسل نسخة JSON كمرفق بريد عبر خدمة إرسال بريد (تحتاج مفتاح خدمة كسرّ على الدالة نفسها).

---

## 11. الأمان **[عام — أهم ما يجب نقله]**
1. **مفتاح `anon` العام مكشوف بطبيعته** في كود الموقع، ولا يملك وصولاً مباشراً للجداول، فقط تنفيذ دوال محددة تتحقق من الهوية. **لا يوضع `service_role` في أي ملف ويصل للمتصفح.**
2. **فحص NULL-safe للأدوار**: `current_user_role()` ترجع NULL لغير المسجّل، فشرط `<> 'admin'` وحده يمرّر مجهولاً؛ لذا يُكتب كل فحص بـ `is distinct from` (ثغرة حقيقية أُصلحت).
3. **PINات ورموز الجلسات مشفرة**: `crypt(pin, gen_salt('bf'))` (bcrypt)، ورموز الجلسة تُخزَّن SHA-256 فقط.
4. **منع التخمين**: قفل الحساب 15 دقيقة بعد 5 محاولات فاشلة (قابلة للضبط بدالتين)، وتسجيل كل محاولة.
5. **جلسة واحدة لكل هوية**: دخول جديد يُسقط الجلسات السابقة لنفس الرمز/الدور (الرئيس لا يُسقط العضو).
6. **RLS** مفعّل على كل الجداول؛ قراءة `competition_state` مقيّدة (كانت `using(true)` لأي مستخدم Auth ثم أُغلقت).
7. **تسريب الجنس**: تُفلتر بيانات المتسابقين حسب جنس اللجنة/المسؤول الفرعي **على الخادم** لا في الواجهة.
8. **استرجاع الرمز/كلمة السر**: صاحب الحساب يكتب بريده (للإدارة) أو رمز دخوله (للجنة/المسؤول) فيُولَّد رمز 6 أرقام **يُرسل للمدير الرئيسي فقط**، الذي يوصله للمستخدم هاتفياً (تحقق هوية يدوي)، ثم يدخل المستخدم الرمز + الجديد؛ التحديث عبر Edge Function بمفتاح خدمة. الرمز مُجزّأ، ينتهي، وله عدّاد محاولات.
9. حماية سباقات الاعتماد (race): حفظ عادي لجلسة معتمدة يُتجاهل بصمت على الخادم؛ قفل يمنع اختبارين لنفس اللجنة؛ حجز ذرّي للمتسابق.
10. الحفظ المتزامن للإدارة **بقفل ودمج** لا استبدال أعمى.
11. الخروج التلقائي بعد 30 دقيقة خمول.

---

## 12. الواجهة **[عام]**
- **شريط جانبي**: نظرة عامة، المتسابقون، إجراء السحب، اختبارات ديوان الحفاظ، سجل السحوبات، مدة الاختبار، مراقبة حية، الإحصاءات، الإعدادات. تُخفى/تُقيَّد بحسب الدور (`applySubAdminRestrictions`, `applySupervisorRestrictions`).
- **شاشة اللجنة** منفصلة: رأس (اسم اللجنة والمستوى والدور)، تبويب السنوية/ديوان الحفاظ، تنبيهات اللجنة، إحصاءات، قائمة المتسابقين بصفحات، وزر تعليمات الاختبار (PDF مرفق `assets/exam-instructions.pdf`).
- عناصر مشتركة: نوافذ Modal، Toast، قوائم منسدلة، فلاتر بحفظ الحالة، تقسيم صفحات مختصر (`renderPagerTabs`)، حلقة نسبة النجاح، حالة المزامنة، وضع فاتح/داكن، وحفظ آخر شاشة فُتحت، ودعم زر الرجوع في المتصفح (`recordBrowserRoute`).
- تنسيق الأرقام لاتيني (`numberingSystem:"latn"`) مع نصوص عربية.

---

## 13. هيكل المشروع
```
index.html            كل الشاشات (بوابة، إعداد، دخول، لوحة إدارة، شاشة لجنة، تجريبي)
app.js                منطق الواجهة والحسابات (المتسابقون، السحب، التقييم، ديوان الحفاظ، التصدير…)
cloud.js              طبقة Supabase: CloudCompetition + DiwanCompetition
styles.css            نظام التصميم (متغيرات CSS، وضع داكن، استجابة، قوالب PDF)
supabase/*.sql        ~45 ملف هجرة بترتيب تنفيذ محدد
supabase/functions/   auto-backup , password-reset (Edge Functions)
data/                 quran.json + بيانات الأسطر (تُحمَّل عند الحاجة)
assets/               شعارات + قوالب PDF الرسمية + 604 صورة صفحة + تعليمات الاختبار
vendor/               مكتبات JS مجمَّعة محلياً
tests/                اختبارات Node بلا تبعيات (حسابات، منطق ديوان الحفاظ، مزامنة…)
supabase-config.js    URL المشروع + مفتاح anon العام فقط (يُنسخ من الـ example)
```
**الاختبارات الآلية**: `node tests/<name>.test.js` لكل ملف (نحو 35 ملفاً)، تغطي الحساب والخصومات والمستويات ومنطق النجاح/الرسوب والمزامنة وسباقات الجلسات وشاشات ديوان الحفاظ.
**نشر التحديثات**: يجب تغيير رقم `?v=` في وسوم `<script>`/`<link>` بـ `index.html` مع كل تعديل على JS/CSS، وإلا يبقى المتصفح على النسخة المخبّأة.

---

## 14. دليل التخصيص لمجال آخر

### ما ينتقل كما هو تقريباً **[عام]**
الأدوار والصلاحيات وتسجيل الدخول برمز+PIN، اللجان برئيس وعضو ومسودتين ومقارنة واعتماد، الحجز الذري، النقل بين اللجان، الحالة الحية والمراقبة، بلاغات اللجان، DR، سجل النشاط، النسخ الاحتياطي، الاستيراد/التصدير، مقارنة العلامات، الأمان (§11)، آليات المزامنة (§2.2)، الاختبار التجريبي، وقوالب PDF فوق صور خلفية بإحداثيات نسبية.

### ما يُستبدل **[خاص بالقرآن]**
| العنصر الحالي | ما تحتاج تحديده في مشروعك |
|---|---|
| المستويات التسعة وعدد الأجزاء | كتالوج مستوياتك وعلاقتها بالفئات/الأعمار |
| بنك المواضع (نص القرآن + الأسطر + 604 صورة) | بنك أسئلتك/مواضيعك (نصوص، صور، ملفات) وكيف يُقسَّم |
| قواعد الخصم (حفظ/لغة/تجويد/تردد/تغيير موضع) وسقف التجويد | أنواع الأخطاء والخصومات وسقوفها ومعادلة العلامة |
| علامة النجاح 75 (سنوي) و85 (ديوان) | حدود النجاح |
| عدد الأسئلة لكل مستوى وقاعدة 15 كحد أقصى | عدد الأسئلة لكل فئة |
| تبديل الموضع: مرتان بخصم 10 | سياسة إعادة/تبديل السؤال |
| مراحل ديوان الحفاظ (10+10+10+نهائي) | إن لم تكن عندك مراحل احذف §7 أو أعد صياغتها |
| الشهادة والتوصية بقوالب المؤنث | قوالبك وحقولها |
| الجنس كمحور للتقسيم بين اللجان | هل تحتاج فصلاً بالجنس أو معياراً آخر |
| الشعارات والأسماء ("جمعية المحافظة على القرآن الكريم — فرع الكورة") | هويتك البصرية |

### أسئلة يجب حسمها قبل البناء
1. هل التقييم بأخطاء وخصومات، أم درجات على محاور، أم قائمة معايير؟
2. هل يوجد رئيس وعضو لكل لجنة أم رقيب واحد؟
3. هل السحب عشوائي من بنك، أم يُعيَّن سؤال يدوياً؟ وهل يُمنع تكراره بين المتسابقين؟
4. هل تحتاج مراحل متتالية أم امتحان واحد؟
5. هل يلزم العمل بلا إنترنت (الوضع المحلي)؟
6. هل يحتاج المشاركون لشهادات/مستندات PDF، وبأي قوالب؟
7. ما الفصل المطلوب بين الفروع/الأجناس/المراكز؟

---

## 15. ملاحق

### 15.1 ثوابت مهمة (`app.js`)
`PASS_SCORE=75` · `DIWAN_PASS_SCORE=85` · `LEVEL_QUESTIONS={3:3,5:3,7:4,10:5,15:8,20:10,25:13,30:15}` · `ASSESSMENT_RULES={حفظ:2, لغة:2, تجويد:1, تردد:0.2, تغيير موضع:10}` · `TAJWEED_ERROR_CAP=10` · `IDLE_LOGOUT_MS=30 دقيقة` · `LIVE_RECENT_WINDOW_MS=12 ساعة` · مؤقت الموضع 20 ثانية · `DIWAN_FINAL_BANDS` (ثلاثة أثلاث) · `DIWAN_RECOMMENDATION_MAX_POINTS=5` · `DIWAN_RECOMMENDATION_MAX_CHARS=70` · حد تبديل الموضع 2 · قفل الدخول 5 محاولات/15 دقيقة · حفظ الإدارة بعد 450ms · استطلاع الإدارة 9 ثوانٍ / اللجنة 5 / عضو اللجنة 1.5 / المراقبة 4 / البلاغات 6.

### 15.2 مفاتيح `localStorage` الرئيسية
`quranCompetition.localBranch.v1` (محلي) · `annualQuranCompetition.v2` (نسخة السحابي) · `diwanAlHifadh.v1` · `trialTest.v1` · `competition-color-mode` · `competition-sidebar-collapsed` · `competition-participant-list-ui` / `competition-diwan-participant-list-ui` (الفلاتر) · `competition-assessment-draft-*` / `diwan-assessment-draft-*` (مسودات التقييم) · `competition-committee-alerts` · `competition-live-autorefresh` · `competition-last-admin-view`.

### 15.3 ملاحظات اتساق وحدود معروفة
- ملف `README.md` الحالي **قديم**: يذكر 15 ملف SQL من أصل نحو 45 ولا يشرح ديوان الحفاظ ولا المراقبة الحية ولا الاختبار التجريبي. هذا الملف (`SITE-SPEC.md`) هو المرجع الأدق.
- نص شاشة السحب يذكر أن المواضع "لا تتكرر" لكن الكود لا يفرض ذلك (انظر §6.4).
- ديوان الحفاظ يستخدم `DIWAN_PASS_SCORE` من الواجهة لتحديد النجاح؛ قاعدة البيانات تقبل أي علامة 0–100 ولا تفرض حد نجاح بنفسها.
- الملفات الأكبر (`app.js`) مكتوبة بأسطر طويلة مكثّفة؛ عند إعادة البناء يُنصح بتقسيمها إلى وحدات (modules) منظمة بدل نسخها كما هي.
